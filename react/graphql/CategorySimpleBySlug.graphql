query CategorySimpleBySlug($categorySlug: String!, $customDomain: String) {
  wpCategories(slug: [$categorySlug], customDomain: $customDomain)
    @context(provider: "vtex.wordpress-integration") {
    categories {
      name
    }
  }
}
